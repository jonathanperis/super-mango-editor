/*
 * editor_layout.h — Editor layout measurement helpers.
 */
#pragma once

#include "editor.h"  /* EditorState */

/* Measure expanded Level Config content height in pixels. */
int editor_config_total_height(const EditorState *es);
